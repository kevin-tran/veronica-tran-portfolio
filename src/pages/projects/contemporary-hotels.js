import React from 'react'

const ContemporaryHotelsPage = ({ transition }) => (
  <div style={transition && transition.style}>
    <h1>Contemporary Hotels</h1>
  </div>
)

export default ContemporaryHotelsPage
